schema_version: 1
name: review
requires_approval: false
steps: [inspect the real diff independently, check rule semantics and HTTP compatibility, verify tests and docs, report findings with exact lines, summarize residual risk]
