schema_version: 1
name: refactor
requires_approval: false
steps: [define preserved behavior and baseline, preserve layer and public contract boundaries, make the smallest structural change, run formatting vet tests and policy, review for accidental behavior changes]
