schema_version: 1
name: bugfix
requires_approval: false
steps: [read the issue and affected contract, reproduce and identify the root cause, add a regression test and the smallest fix, run formatting vet tests and policy, review compatibility]
