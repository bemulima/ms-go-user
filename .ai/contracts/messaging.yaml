schema_version: 1
documentation: docs/messaging-contract.md
core_nats_rpc:
  consumes: [{subject: user.create-user, queue: ms-go-user}]
  produces: [auth.verifyJWT, rbac.checkRole]
user_create_semantics: idempotent user/profile creation; optional OAuth profile fills missing values; avatar failure returns warning
